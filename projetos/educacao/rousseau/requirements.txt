# Sistema Rousseau - dependências
# Python 3.11+ recomendado

# Separação silábica em português
pyphen>=0.14

# Renderização da notação
svgwrite>=1.4
reportlab>=4.0

# Interface web (opcional, para os componentes visuais)
streamlit>=1.30

# Download de áudio do YouTube
# (conversão para MP3 requer ffmpeg instalado no sistema; sem ele o áudio
# é salvo no formato nativo m4a/webm)
yt-dlp>=2025.1.1

# Transcrição automática de melodia (pYIN)
librosa>=0.11
soundfile>=0.12
